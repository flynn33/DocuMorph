# DocuMorph

**Current Version: 2.0.0** <!-- x-release-please-version -->

DocuMorph is a macOS SwiftUI app that batch-converts documents from a source folder into human-readable Markdown (`.md`) or structured JSON (`.json`) in a target folder.

Built with the [Forsetti Framework](https://github.com/jdaley/Forsetti-Framework) — a proprietary modular Swift runtime framework for native Apple applications.

## Table of Contents

- [What It Does](#what-it-does)
- [Built with the Forsetti Framework](#built-with-the-forsetti-framework)
- [Supported Formats](#supported-formats)
- [Output Design](#output-design)
- [Architecture](#architecture)
- [Requirements](#requirements)
- [Quick Start](#quick-start)
- [How to Use](#how-to-use)
- [Build From Terminal](#build-from-terminal)
- [Project Structure](#project-structure)
- [Extending with New Converters](#extending-with-new-converters)
- [Known Limitations](#known-limitations)
- [Troubleshooting](#troubleshooting)
- [Contributing](#contributing)
- [License](#license)
- [Changelog](#changelog)

## What It Does

- Converts supported files from one folder to another in bulk.
- Supports two output formats:
  - Markdown for direct reading/editing.
  - JSON for downstream automation/LLM/data pipelines.
- Produces readable output:
  - Paragraph normalization.
  - Heading/list detection.
  - Page segmentation for PDFs.
  - Pretty-printed, sorted JSON.

## Built with the Forsetti Framework

DocuMorph is built on the **Forsetti Framework v0.1.0**, a proprietary modular runtime framework for iOS and macOS applications created by James Daley. Forsetti provides:

- **Module-based architecture**: DocuMorph is implemented as a `ForsettiAppModule` — a single-module app where the entire application (UI + logic) lives in one module and the framework runs silently in the background.
- **Runtime lifecycle management**: Module discovery, compatibility validation, and activation are handled by the Forsetti runtime.
- **Service abstraction**: Platform services (storage, file export) are accessed through Forsetti's protocol-based service container.
- **Deployment Pattern A**: Single-module production deployment where end users interact only with the DocuMorph UI; framework controls are hidden.

The Forsetti Framework is included as a vendored local Swift Package in the `Packages/ForsettiFramework/` directory. It is proprietary software — see [License](#license) for details.

## Supported Formats

| Input Extension | Converter | Notes |
| --- | --- | --- |
| `pdf` | `PDFConverter` | Text extracted page-by-page using `PDFKit`. |
| `docx`, `doc`, `rtf`, `odt`, `html`, `htm` | `AttributedDocumentConverter` | Text extracted via `NSAttributedString` document importers. |
| `txt` | `TextConverter` | UTF-8 text input. |

## Output Design

### Markdown output

Markdown output is generated by shared formatting logic and includes:

- Top-level title derived from input filename.
- Page headings for PDFs (`## Page N`).
- Converted headings and list items when detectable.
- Merged wrapped lines into readable paragraphs.

### JSON output

JSON is emitted as a readable document model (`version: "1.0"`) with:

- `source` metadata: `fileName`, `fileExtension`, `convertedAt` (ISO-8601 UTC timestamp)
- `summary` metadata: `blockCount`, `pageCount` (present for paged content like PDF)
- `content` payload: `blocks` for single-stream documents, `pages` for paged documents

## Architecture

DocuMorph follows the Forsetti Framework's modular architecture:

- **`DocuMorphModule`** (`ForsettiAppModule`):
  - The complete application module containing all conversion logic and UI.
  - Registered via `DocuMorphModuleRegistry` and discovered at runtime from its JSON manifest.
  - Provides UI contributions (view injection) to the Forsetti host.
- **`DocuMorphBootstrap`**:
  - Initializes the Forsetti runtime, module registry, and view injection registry.
  - Configures the host controller with `showDeveloperControls: false` for production deployment.
- **`ContentView`** (SwiftUI):
  - Collects source/target folder paths.
  - Selects output format.
  - Triggers conversion.
- **`ConversionManager`**:
  - Enumerates files in source folder.
  - Routes each file to the first matching converter by extension.
  - Writes output with `.md` or `.json` extension.
- **`DocumentConverter`** protocol:
  - Shared interface for all converters.
- **`ReadableOutputFormatter`**:
  - Shared formatter for Markdown and JSON output consistency.
  - Applies structural heuristics (heading/list/paragraph detection).

## Requirements

- macOS 14+ (AppKit and PDFKit are used directly).
- Xcode 16+ (project uses Swift 5 settings with Forsetti Framework local package).
- No external package dependencies beyond the vendored Forsetti Framework.

Current project settings in `DocuMorph.xcodeproj`:
- `MARKETING_VERSION = 2.0`
- `CURRENT_PROJECT_VERSION = 1`

## Quick Start

1. Clone the repository.
2. Open `DocuMorph.xcodeproj` in Xcode.
3. Xcode will automatically resolve the local Forsetti Framework package from `Packages/ForsettiFramework/`.
4. Select the `DocuMorph` scheme.
5. Build and run the app.

## How to Use

1. Launch the app.
2. Click **Select** next to **Source Folder** and choose input directory.
3. Click **Select** next to **Target Folder** and choose output directory.
4. Choose **Markdown** or **JSON**.
5. Click **Convert Files**.
6. Inspect output files in target directory.

Behavior notes:

- Unsupported file extensions are skipped silently.
- Output filename preserves the original base name and changes only the extension:
  - `example.pdf` -> `example.md` or `example.json`

## Build From Terminal

```bash
xcodebuild -project "DocuMorph.xcodeproj" \
  -scheme "DocuMorph" \
  -configuration Debug \
  -sdk macosx \
  build
```

## Project Structure

```text
DocuMorph/
├── Packages/
│   └── ForsettiFramework/          # Vendored Forsetti Framework (sealed dependency)
│       ├── Package.swift
│       └── Sources/
├── DocuMorph/
│   ├── DocuMorphApp.swift          # App entry point (Forsetti host)
│   ├── DocuMorphBootstrap.swift    # Forsetti runtime bootstrap
│   ├── ContentView.swift           # Main conversion UI
│   ├── Module/
│   │   ├── DocuMorphModule.swift         # ForsettiAppModule implementation
│   │   └── DocuMorphModuleRegistry.swift # Module factory registration
│   ├── Resources/
│   │   └── ForsettiManifests/
│   │       └── DocuMorphModule.json      # Module manifest
│   ├── OutputFormat.swift
│   ├── ConversionManager.swift
│   ├── DocumentConverter.swift
│   ├── PDFConverter.swift
│   ├── AttributedDocumentConverter.swift
│   ├── TextConverter.swift
│   └── ConversionError.swift
├── DocuMorph.xcodeproj/
├── README.md
├── CONTRIBUTING.md
├── LICENSE.md
├── CHANGELOG.md
└── wiki.md
```

## Extending with New Converters

1. Create a new type that conforms to `DocumentConverter`.
2. Declare `supportedExtensions`.
3. Implement `convert(at:to:)`.
4. Prefer `ReadableOutputFormatter` for consistent Markdown/JSON output.
5. Register the converter in `ConversionManager` initializer.
6. Mark the class as `final` per Forsetti OOP guidelines.

## Known Limitations

- Formatting is heuristic-based, not layout-faithful.
- PDF extraction quality depends on selectable text layer; scanned image PDFs need OCR (not currently included).
- Tables, images, footnotes, and advanced styles may flatten to plain text.
- `TextConverter` currently assumes UTF-8 input.
- Unsupported extensions are skipped without an on-screen per-file report.

## Troubleshooting

- Build fails with SDK/deployment mismatch:
  - Align installed Xcode/macOS SDK with project deployment target.
  - Or lower deployment target in Build Settings and re-run.
- Forsetti package resolution fails:
  - Ensure the `Packages/ForsettiFramework/` directory exists and contains `Package.swift`.
  - In Xcode, go to File > Packages > Reset Package Caches.
- Empty or sparse output from PDF:
  - Confirm the PDF contains selectable text (not just images).
- Output not appearing:
  - Verify folder permissions and chosen target directory.
  - Check whether files in source directory use supported extensions.

## Contributing

See [CONTRIBUTING.md](CONTRIBUTING.md).

## License

See [LICENSE.md](LICENSE.md).
This project is distributed under a proprietary internal-use license. The Forsetti Framework included in this project is separately licensed proprietary software owned by James Daley.

## Changelog

See [CHANGELOG.md](CHANGELOG.md).
